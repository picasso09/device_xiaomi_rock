# Allow health HAL to read vendor_sysfs_battery_supply
r_dir_file(hal_health_default, sysfs_power_supply)

# Allow health HAL to read sysfs
r_dir_file(hal_health_default, sysfs)
r_dir_file(hal_health_default, sysfs_wakeup)

